schema_version: "1.1"
name: comment-versus-body
tags: [units, layer2]
runs: 3
context:
  scaffold_script: scaffold.sh
execution:
  prompt: |
    This project is at `MyGame`. A designer wants the reach distance in centimetres for a
    trace. What value does `GetReachDistance` return, in centimetres?
  max_turns: 8
  timeout_seconds: 180
  allowed_tools: [Read, Glob, Grep]
