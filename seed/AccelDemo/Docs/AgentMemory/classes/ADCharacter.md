# ADCharacter

Module `AccelDemo`. Generated by the AgentMemoryDump commandlet, do not hand edit.

Declared in `Public/ADCharacter.h`.
Line numbers are not available from reflection; grep that header for a member name.

- Inherits: Character -> Pawn -> Actor -> Object
- Implements: ADInteractable

## Summary

6 functions, 5 properties, 2 replicated.

| Replicated property | Type | Condition | OnRep |
|---|---|---|---|
| `DeathCount` | `int32` | COND_SkipOwner | - |
| `Health` | `float` | COND_OwnerOnly | OnRep_Health |

### Functions

**Effective specifiers**, as UnrealHeaderTool computed them, read from the live
`UFunction`. These can differ from the specifier list written in the header and the
table is right when they do: a const `BlueprintCallable` with a return value is
`BlueprintPure` here and is not marked so in the header. **This table wins over the
header. A disagreement is not a defect in either.**

| Function | Signature | Specifiers | Doc |
|---|---|---|---|
| `ApplyDamage` | `void ApplyDamage(float Amount)` | BlueprintCallable, Native | Applies damage to this character. Server only. Always call this rather than setting Health directly: Health is replicated with a condition, and writing to it outside this function skips the replication bookkeeping, so clients will keep the stale value until some unrelated property forces a resend. |
| `ClientNotifyDeath` | `void ClientNotifyDeath()` | Native, Net, Client, Reliable |  |
| `IsAlive` | `bool IsAlive()` | BlueprintCallable, BlueprintPure, Native |  |
| `MulticastPlayHitReaction` | `void MulticastPlayHitReaction(float Amount)` | Native, Net, NetMulticast |  |
| `OnRep_Health` | `void OnRep_Health()` | Native |  |
| `ServerRequestRespawn` | `void ServerRequestRespawn()` | Native, Net, Server, Reliable |  |

### Properties

| Property | Type | Specifiers | Replication | Doc |
|---|---|---|---|---|
| `DeathCount` | `int32` | BlueprintReadOnly, Replicated | COND_SkipOwner |  |
| `Health` | `float` | BlueprintReadOnly, Replicated, ReplicatedUsing | COND_OwnerOnly, OnRep=OnRep_Health |  |
| `MaxHealth` | `float` | EditAnywhere, BlueprintReadWrite |  |  |
| `RespawnDelaySeconds` | `float` | EditAnywhere, EditDefaultsOnly |  |  |
| `Stamina` | `TObjectPtr<UADStaminaComponent>` | EditAnywhere, EditConst, BlueprintReadOnly |  |  |

