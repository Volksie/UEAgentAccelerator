# ADInteractable

Module `AccelDemo`. Generated by the AgentMemoryDump commandlet, do not hand edit.

Declared in `Public/ADInteractable.h`.
Line numbers are not available from reflection; grep that header for a member name.

- Inherits: Interface -> Object

## Summary

1 function, 0 properties, 0 replicated.

No property declared on this class replicates.

### Functions

**Effective specifiers**, as UnrealHeaderTool computed them, read from the live
`UFunction`. These can differ from the specifier list written in the header and the
table is right when they do: a const `BlueprintCallable` with a return value is
`BlueprintPure` here and is not marked so in the header. **This table wins over the
header. A disagreement is not a defect in either.**

| Function | Signature | Specifiers | Doc |
|---|---|---|---|
| `GetInteractionPrompt` | `FText GetInteractionPrompt()` | BlueprintCallable, BlueprintPure, BlueprintEvent, Native | Returns the text shown on the interaction prompt. Blueprints may override this; the C++ default lives in each implementing class. |

