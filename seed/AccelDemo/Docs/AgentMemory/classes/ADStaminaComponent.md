# ADStaminaComponent

Module `AccelDemoCore`. Generated by the AgentMemoryDump commandlet, do not hand edit.

Declared in `Public/ADStaminaComponent.h`.
Line numbers are not available from reflection; grep that header for a member name.

- Inherits: ActorComponent -> Object

## Summary

5 functions, 5 properties, 2 replicated.

| Replicated property | Type | Condition | OnRep |
|---|---|---|---|
| `bExhausted` | `bool` | COND_None | OnRep_Exhausted |
| `Stamina` | `float` | COND_OwnerOnly | - |

- Doc: Tracks a pawn's stamina pool and its regeneration. See Docs/Design/StaminaSystem.md for why regeneration pauses after damage.

### Functions

**Effective specifiers**, as UnrealHeaderTool computed them, read from the live
`UFunction`. These can differ from the specifier list written in the header and the
table is right when they do: a const `BlueprintCallable` with a return value is
`BlueprintPure` here and is not marked so in the header. **This table wins over the
header. A disagreement is not a defect in either.**

| Function | Signature | Specifiers | Doc |
|---|---|---|---|
| `ApplyStamina` | `void ApplyStamina(float Amount)` | BlueprintCallable, Native | Adds Amount to the current stamina pool. Amount is in stamina points, not a percentage, so passing 50 adds half the default pool rather than half of what remains. Values that would take the pool outside 0..100 are clamped, and no warning is issued for out of range input. |
| `GetReachDistance` | `float GetReachDistance()` | BlueprintCallable, BlueprintPure, Native | Returns how far this pawn can reach to interact with something, in metres. |
| `GetStamina` | `float GetStamina()` | BlueprintCallable, BlueprintPure, Native |  |
| `NotifyDamaged` | `void NotifyDamaged()` | BlueprintCallable, Native | Call this whenever the owner takes damage. It restarts the regeneration pause, which is why damage has to tell the component rather than the component watching health. |
| `OnRep_Exhausted` | `void OnRep_Exhausted()` | Native |  |

### Properties

| Property | Type | Specifiers | Replication | Doc |
|---|---|---|---|---|
| `bExhausted` | `bool` | BlueprintReadOnly, Replicated, ReplicatedUsing | COND_None, OnRep=OnRep_Exhausted |  |
| `MaxStamina` | `float` | EditAnywhere, BlueprintReadWrite |  |  |
| `RegenPauseSeconds` | `float` | EditAnywhere, BlueprintReadOnly |  | How long regeneration stays paused after damage. See Docs/Design/StaminaSystem.md for where the 2 came from; it is a playtest result rather than a technical constraint. |
| `RegenPerSecond` | `float` | EditAnywhere, BlueprintReadOnly |  | Stamina restored per second once regeneration resumes. Tuned against the 2 second post-damage pause, so changing one without the other breaks the recovery curve the design doc describes. |
| `Stamina` | `float` | BlueprintReadOnly, Replicated | COND_OwnerOnly | Current stamina, 0..MaxStamina. Replicated to the owning client only; other clients never see this value and must not branch on it. |

