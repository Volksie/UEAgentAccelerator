# AgentMemoryDumpCommandlet

Module `UEAgentAcceleratorTools`. Generated by the AgentMemoryDump commandlet, do not hand edit.

Declared in `Public/AgentMemoryDumpCommandlet.h`.
Line numbers are not available from reflection; grep that header for a member name.

- Inherits: Commandlet -> Object

## Summary

0 functions, 0 properties, 0 replicated.

No property declared on this class replicates.

- Doc: Emits one Markdown file per module describing the live reflection surface: class hierarchy, interfaces, UFUNCTION and UPROPERTY specifiers, replication conditions, and the doc comments UHT captured as metadata.  The output is deterministic and uses no absolute paths, so it can be committed and shared. It is the only layer that can answer COND_ questions, because those live in GetLifetimeReplicatedProps at runtime and appear in no header.  Run with:   UnrealEditor-Cmd.exe <project>.uproject -run=UEAgentAcceleratorTools.AgentMemoryDump [-Modules=A,B] [-Out=Docs/AgentMemory]

