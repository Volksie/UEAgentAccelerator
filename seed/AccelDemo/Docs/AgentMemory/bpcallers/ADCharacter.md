# Blueprint users of `ADCharacter`

Generated by the AgentMemoryDump commandlet. Do not hand edit.

3 use(s) from Blueprint graphs, resolved through the node's member
reference rather than matched by name. Renaming or changing the signature of any
symbol listed here still compiles clean, and breaks these graphs.

**A negative here is real.** This is a resolved graph walk, not a text search, so a
symbol absent from this table has no Blueprint users of the kinds the walk covers: graph
nodes (`call`, `read`, `write`) in event, function and macro graphs, and `bind`, a
`BindWidget`/`BindWidgetAnim` property bound by name in a widget's designer tree. Data-only
Blueprints have no graphs and cannot appear here; that is not a gap. Not covered: uses of a
*class* rather than a member, such as component templates and variable types. Do not fall
back to ripgrep or `strings` on a `.uasset` to double-check: neither can see these
references, and both will report nothing whether or not a caller exists.

| C++ symbol | Used by | Graph | How |
|---|---|---|---|
| `ADCharacter::ApplyDamage` | `/Game/Bench/BP_AccelDemoCharacter` | EventGraph | call |
| `ADCharacter::IsAlive` | `/Game/Bench/BP_AccelDemoCharacter` | EventGraph | call |
| `ADCharacter::MaxHealth` | `/Game/Bench/BP_AccelDemoCharacter` | EventGraph | read |
