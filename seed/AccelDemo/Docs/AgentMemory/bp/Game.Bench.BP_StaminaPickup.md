# `/Game/Bench/BP_StaminaPickup`

Generated by the AgentMemoryDump commandlet. Do not hand edit.

Parent `/Script/Engine.Actor`, ultimate native parent `/Script/Engine.Actor`.

**Every value in this file is also a field in `blueprints.jsonl`**, which is the
same data as one line of JSON. Query that rather than parsing this.

## Components and widgets

Root: `DefaultSceneRoot`. Inherited ones are listed too, with where they come from -
"does this have a camera" does not care who declared it.

| Name | Class | Kind | Attached to | From |
|---|---|---|---|---|
| DefaultSceneRoot | SceneComponent | component | - | declared here |

## Tick

- Can ever tick: **no**
- Starts with tick enabled: **yes**
- Event Tick has anything connected: **no**

## Functions

| Name | Inputs | Outputs | Pure | Const | Access |
|---|---|---|---|---|---|
| UserConstructionScript | - | - | no | no | public |

## Events

**Connected** says whether the event's exec pin actually goes anywhere. The editor
adds empty `ReceiveBeginPlay`, `ReceiveTick` and `ReceiveActorBeginOverlap` nodes to
a new Blueprint, so an event listed here with **no** is a node that exists and does
nothing - not an implementation.

| Name | Kind | Connected | Replication | Inputs |
|---|---|---|---|---|
| ReceiveActorBeginOverlap | override | no | - | OtherActor : Actor Object Reference |
| ReceiveBeginPlay | override | no | - | - |
| ReceiveTick | override | no | - | DeltaSeconds : Float (single-precision) |

## Interfaces

| Interface | Functions implemented here |
|---|---|
| ADInteractable | GetInteractionPrompt |
